Напишите код, помещающий в переменную int c количество четных элементов c четными индексами ранее созданного массива a.

Верный по форме, но неверный по содержанию ответ:

int с = a[0];


Ответ:
int c = 0;
for (int i = 0; i < a.length; i += 2) {
    if (a[i] % 2 == 0) {
    c++;
    }
}